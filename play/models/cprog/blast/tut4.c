#include <assert.h>

int main() {
  int i;
  int x;
  int y;
  int ctr;

  x = ctr;
  y = x;
  y = x + 1;
  //y = ctr + 1; 
  
  if (ctr == i) {
    if (y != i + 1) {
    ERROR: goto ERROR;    
    //assert (y == i + 1); 
    }  
  }
}
